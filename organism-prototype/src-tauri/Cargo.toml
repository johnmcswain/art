[package]
name = "living-field"
version = "0.1.0"
description = "A responsive particle organism"
authors = ["you"]
edition = "2021"

[lib]
name = "living_field_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
